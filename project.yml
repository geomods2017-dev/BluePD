name: BluePD

options:
  bundleIdPrefix: com.bluepd

targets:
  BluePD:
    type: application
    platform: iOS
    deploymentTarget: "17.0"

    sources:
      - path: BluePD/BluePD

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bluepd.app
        INFOPLIST_FILE: BluePD/Info.plist
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_ENTITLEMENTS: BluePD/BluePD.entitlements
        DEVELOPMENT_TEAM: ""  # leave empty if Codemagic handles signing

    info:
      path: BluePD/Info.plist
      properties:
        CFBundleDisplayName: BluePD
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
